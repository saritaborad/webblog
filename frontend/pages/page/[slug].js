import Base from "@/layouts/Baseof";

const BlogPagination = ({ posts, authors, currentPage, pagination }) => {
 return (
  <Base>
   <section className="section">{/* <div className="container">Home page</div> */}</section>
  </Base>
 );
};

export default BlogPagination;
